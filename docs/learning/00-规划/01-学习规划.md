# opencode 学习规划

> 目标：从全局到细节，系统地理解 opencode 的架构与实现，最终服务于「把它定制改造为 openhive」。
> 说明：本规划是**总纲**，只讲路线。各层和专题的具体内容都在独立笔记里（见文末清单），按「层文件夹」组织。

---

## 一、学习目标与最终形态

1. **理解层**：能说清楚 opencode 是什么、解决什么问题、整体架构如何分层。
2. **机制层**：能讲透核心运行时的数据流（输入 → 组装上下文 → 调模型 → 工具调用 → 落库）。
3. **改造层**（终极目标）：能定位「品牌化改造点」，并判断每个改动点「改哪里、怎么改才能最小化与上游的合并冲突」。

改造层是落脚点——因为本仓库是官方 opencode 的 fork，**必须能持续同步上游 `dev`**，这是贯穿全程的最重要约束。

---

## 二、全局地图（先建立心智模型）

opencode 是 **Bun monorepo**（30+ 包），本质是「终端里的 AI 编程助手」（对标 Claude Code / Cursor）。理解时只需记住三层：

```
产品层（怎么用）
  ├─ packages/app        网页 UI（SolidJS）
  ├─ packages/desktop    桌面端
  ├─ packages/tui        终端 UI（opentui）
  └─ packages/cli        命令行入口

运行时层（核心，90% 精力在这里）
  └─ packages/opencode   主程序，src/ 下 40+ 模块：
        session  agent  provider  tool  permission  config
        mcp  lsp  plugin  server  snapshot  sync  storage ...

支撑层（库/协议，相对稳定）
  ├─ packages/core        领域模型 + 持久化表定义（**/sql.ts）
  ├─ packages/schema      领域类型（session/workspace/location 等）
  ├─ packages/protocol    协议定义
  ├─ packages/llm         模型适配（封装 AI SDK）
  ├─ packages/server      服务端（Hono）
  └─ packages/client/sdk/sdk-next  客户端与 SDK
```

**依赖方向是单行道**（`AGENTS.md` 写死）：
`Schema → Core/Protocol → Server`；`Client` 只依赖 Schema+Protocol，不碰 Core/Server；`sdk-next` 组合三者。看到反向依赖即为异类。

**技术栈关键词**：Effect（函数式框架，`effect@4.0-beta`）、AI SDK（`ai@6.0`，模型适配）、Hono（HTTP）、Drizzle ORM + SQLite（持久化）、SolidJS（UI）、opentui（TUI）。

---

## 三、六层递进路径（均已产出笔记）

| 层 | 目标 | 产出笔记 |
|---|---|---|
| **0. 产品认知** | 建立「它是什么、解决什么」 | `01-产品认知/01-产品认知.md` |
| **1. 工程结构** | 看懂 monorepo 组织与构建 | `02-工程结构/01-工程结构.md` |
| **2. 术语表 + 核心概念** | 过「语言关」（**最关键**） | `03-核心概念/01-核心概念.md` + `02-工作目录.md` |
| **3. 跟一条数据流** | 把概念落到真实代码路径 | `04-数据流/`（5 篇） |
| **4. 逐子系统深挖** | 按依赖顺序吃透模块 | `05-子系统/`（7 篇） |
| **5. 改造视角** | 理解 →「知道改哪、怎么改不冲突」 | `06-改造地图/01-改造地图.md` |

---

## 四、两个横切专题（已独立成篇）

除六层路径外，还有两个跨层专题，已拆成独立笔记：

| 专题 | 位置 | 内容 |
|---|---|---|
| 工作目录 | `03-核心概念/02-工作目录.md` | Location / Project / Workspace 三层概念 |
| 持久化 | `05-子系统/07-持久化专题.md` | 数据存哪、两套存储、18 张数据表全景 |

---

## 五、贯穿全程的方法论

1. **术语先行**：opencode 有一套自己的词汇（Context Source / Context Epoch / Provider Turn / Session Drain / Tool Registry…），`CONTEXT.md` 给了每个词的准确定义和「避免叫法」。**不过这一关，读 session 代码如读天书**。
2. **跟数据流，别背目录**：AI 编程工具本质是一条循环——`用户输入 → 组装上下文 → 调模型 → 模型要工具调用 → 工具改文件/跑命令 → 结果写回会话 → 再调模型`。顺流读比按字母序读模块高效得多。

---

## 六、注意事项与坑

1. **V2 vs legacy 并存**：项目正在做 V2 Session Core 重构，V2（`SessionV2`、`SessionExecution`、`event` 表）与 legacy（`SessionPrompt.loop`、`message`/`part` 表）并存。读 `session/` 先判断是哪种。
2. **依赖方向**：Schema → Core/Protocol → Server，Client 不依赖 Core/Server。
3. **fork 改造约束**（`CLAUDE.md`）：品牌化优先用配置/env；每次改动单独提交、写清「这是要保留的定制」；定期 `git fetch upstream && git merge upstream/dev`。
4. **同步方向永远是 `upstream/dev → main`**，learning 分支只 push 到 `origin`、从不 push `upstream`。

---

## 七、进度追踪清单

- [x] 层 0：产品认知 —— `01-产品认知/01-产品认知.md`
- [x] 层 1：工程结构 —— `02-工程结构/01-工程结构.md`
- [x] 层 2：核心概念 —— `03-核心概念/`（含工作目录专题）
- [x] 层 3：数据流 trace —— `04-数据流/`（5 篇）
- [x] 层 4：子系统深挖 —— `05-子系统/`（7 篇，含持久化专题）
- [x] 层 5：改造地图 —— `06-改造地图/01-改造地图.md`

**全部六层 + 两个专题已学习完毕。** 下一步是从 `06-改造地图` 出发，进入真正的动手改造。
